A simple CRUD application using React as the frontend and Spring Boot as the backend. Let's use the MySQL database to store and retrieve the data.

In this I will use the latest version of React 18+ and Spring Boot version 3+.
